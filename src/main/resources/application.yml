spring:
  application:
    name: forum
  datasource:
    url: jdbc:mysql://localhost/forum
    username: root
    password: caelum
    driverClassName: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      format-sql: true
  rabbitmq:
    host: 192.168.1.9
    port: 5672
    username: guest
    password: guest

services:
  url:
    courses: bla
    users: bla2
